# React Native / Expo require a flat node_modules: Metro and native autolinking
# don't reliably follow pnpm's symlinked store, and symlinks let workspace
# packages (e.g. @medcopilot/ui) resolve a *separate* physical copy of
# react-native, producing two bridge instances ("No callback found with cbID").
node-linker=hoisted
